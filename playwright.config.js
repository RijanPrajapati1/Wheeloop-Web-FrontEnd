// playwright.config.js

module.exports = {
    testDir: './tests', // The directory where your test files are located
    testMatch: '**/*.test.jsx', // Match files with .test.jsx extension
    use: {
        // Add any global settings here if needed, e.g. baseURL, browser context, etc.
    },
};
